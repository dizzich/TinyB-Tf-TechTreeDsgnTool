# TechTree Studio - Швидкий старт

## 🚀 Найпростіший спосіб запустити

### Варіант 1: Автоматичний запуск (Рекомендовано)

Просто двічі клацніть на файл:
```
START.bat
```

Цей скрипт:
- Перевірить наявність Node.js
- Встановить залежності (якщо потрібно)
- Запустить сервер розробки
- Відкриє браузер на http://localhost:5173

### Варіант 2: Вручну через командний рядок

1. Відкрийте термінал в цій папці
2. Встановіть залежності (тільки перший раз):
   ```bash
   npm install
   ```
3. Запустіть сервер:
   ```bash
   npm run dev
   ```
4. Відкрийте браузер: http://localhost:5173

---

## 📦 Створення автономної версії

Щоб отримати версію, яку можна відкрити просто подвійним кліком на HTML:

### Крок 1: Збірка production версії

Двічі клацніть на:
```
BUILD.bat
```

Або вручну:
```bash
npm run build
```

### Крок 2: Використання

Після збірки з'явиться папка `dist` з файлами:
- `dist/index.html` - головний файл
- `dist/assets/` - всі ресурси (JS, CSS)

**Важливо:** Через обмеження браузерів, просто відкрити `index.html` може не працювати коректно.

### Крок 3: Запуск автономної версії

**Варіант A - Локальний сервер (Найкраще):**
```bash
npx serve dist
```
Потім відкрийте http://localhost:3000

**Варіант B - Python HTTP сервер:**
```bash
cd dist
python -m http.server 8000
```
Потім відкрийте http://localhost:8000

**Варіант C - Подвійний клік (Обмежено):**
Просто відкрийте `dist/index.html` в браузері.
⚠️ File System Access API не буде працювати (тільки fallback для завантаження файлів)

---

## 🎯 Рекомендації

### Для розробки:
Використовуйте `START.bat` - це найшвидший спосіб

### Для демонстрації:
Зберіть production версію і запустіть через `npx serve dist`

### Для передачі іншим:
1. Виконайте `BUILD.bat`
2. Скопіюйте папку `dist`
3. Дайте інструкцію запускати через `npx serve dist`

---

## 📋 Системні вимоги

- **Node.js** 18 або новіше (завантажити з https://nodejs.org/)
- **Браузер**: Chrome, Edge, Firefox (Chrome рекомендовано для File System Access API)
- **ОС**: Windows 10/11, macOS, Linux

---

## 🐛 Усунення проблем

### "Node.js is not installed"
Встановіть Node.js з https://nodejs.org/

### "npm is not recognized"
Перезапустіть термінал після встановлення Node.js

### Порт 5173 зайнятий
Vite автоматично використає наступний вільний порт (5174, 5175, і т.д.)

### Білий екран після збірки
Переконайтеся що запускаєте через HTTP сервер, а не просто відкриваєте файл

### File System Access не працює
Використовуйте Chrome або Edge та запускайте через HTTP (не file://)

---

## 📞 Підтримка

Для питань та проблем створіть issue в репозиторії проекту.
